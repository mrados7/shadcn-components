import { Button } from "@/main";
import React from "react";
import "./index.css"

interface IProps {}

const App: React.FC<IProps> = () => {
  return <div>
    <Button variant="default">Ivafn</Button>
  </div>;
};

export default App;
